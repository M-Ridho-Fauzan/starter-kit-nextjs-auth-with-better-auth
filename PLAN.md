# PLAN.md — Living Document

> This file is updated every session. Architect writes here. Builder reads here.
> Do not treat old entries as current — always check "Current Phase" section.

---

## Current Phase

**Phase 6 — DX & Documentation**
Status: COMPLETE
All 6 phases of the project are now fully implemented.

See AGENTS.md checklist for the full list of completed items.

---

## Session Log

### Session 1 — Phase 1 Foundation Design (completed)

- Resolved PD-001, PD-002, PD-003 → output to `.agents/decisions/phase1-foundation-design.md`
- Designed full TypeScript types for `defineAuthConfig` with all sections
- Builder implemented Phase 1 item 1: `auth.config.ts` schema + TypeScript types
  - Created: schema.ts, types.ts, index.ts, index.test.ts (10 source files)
  - All 16 tests pass, typecheck clean
- **Next**: Resolve PD-004 before Phase 1 item 2

### Session 2 — PD-004 Resolution (current)

- Resolved PD-004 (Distribution Model) → output to `.agents/decisions/pd-004-core-setup-design.md`
- Decision: Option (b) — CLI installer with template
- Determined singleton pattern for `server.ts`, generated route handler & auth-client
- **Next session**: Build Phase 1 item 2 — Better Auth core setup

### Session 3 — Phase 6 DX & Documentation (completed)

- Implemented all Phase 6 items (Auto-generated docs, master index, .env.example generator, setup wizard CLI, README)
- Created `scripts/lib/generate-env.ts` — collects env vars from config, generates `.env.example`
- Created `scripts/lib/generate-env.test.ts` — 11 tests
- Created `scripts/generate-env.example.ts` — entry point for `pnpm generate-env`
- Created `scripts/lib/write-config.ts` — generates `auth.config.ts` via template strings
- Created `scripts/lib/write-config.test.ts` — 18 tests
- Created `scripts/setup.ts` — interactive wizard with 17 prompt steps
- Created `scripts/setup.test.ts` — 4 tests
- Created `README.md` — full public-facing documentation
- Generated `.env.example` from current `auth.config.ts`
- All 166 tests passing (20 files), typecheck clean
- **Project complete** — all 6 phases delivered

### Session 0 — Project Setup (archived)

- Initialized project structure
- Configured opencode.jsonc with plan/build agents
- Installed skills: create-auth-skill, docs-writing, find-skills
- Defined AGENTS.md with full checklist and code standards

---

## Pending Decisions (architect to resolve)

_None — all pending decisions resolved._

---

## Architecture Decisions Log (completed)

### PD-001: Config validation strategy

**Decision: Option (c) — Zod for runtime validation + TypeScript types derived via `z.infer`**

See `.agents/decisions/phase1-foundation-design.md` for full rationale.

### PD-002: Adapter interface design

**Decision: Option (B) — Factory function returning adapter object**

See `.agents/decisions/phase1-foundation-design.md` for full rationale.

### PD-003: Server/client code split

**Decision: `defineAuthConfig` is purely server-side. The `ui` section is metadata consumed by client helpers.**

See `.agents/decisions/phase1-foundation-design.md` for full details.

### PD-004: Distribution Model

**Decision: Option (b) — CLI installer (`create-next-auth-starter`) with a template repo.**

Implications for Phase 1 item 2:
- Singleton pattern for `server.ts` (imports `auth.config.ts` statically)
- Route handler & auth-client generated by CLI, not written by user
- New file: `src/auth/mapper.ts` to translate `AuthConfig` → `BetterAuthOptions`

See `.agents/decisions/pd-004-core-setup-design.md` for full details.

---

## Current Feature

All features complete. Project is ready for use.

---

## Blockers

None.

---

## Notes for Next Session

Project is complete. Next steps for maintainers:
- Publish as npm package (`create-next-auth-starter`)
- Add CI/CD pipeline (GitHub Actions)
- Add more OAuth providers (Discord, Twitter, etc.)
- Add rate limiting
- Add audit logging
- Performance benchmarks
